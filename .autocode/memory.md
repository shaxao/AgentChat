# AutoCode 项目记忆

这个文件用于保存长期有效的项目事实、用户偏好和已经确认过的决策。

## 已确认事实
- 当前仓库默认分支为 `main`，远程 `origin` 指向 `https://github.com/shaxao/AgentChat.git`。
- 最新提交 `bb284be`（`docs: 同步两处 README 为中文，版本号修正为 0.4.12，补充技术栈与开发说明`）已推送到 `origin/main`。
- 当前 `git status --short --branch` 结果为干净状态：`## main...origin/main`，本地与远程同步。
- Windows cmd 不支持 heredoc 语法，提交消息含中文/特殊字符时应写入临时文件再用 `git commit -F` 读取，提交后及时删除临时文件。
- 已移除误提交的临时文件 `.git-commit-msg.txt`。

## 用户偏好
- 

## 决策记录
-